# Server
server.port=8081

# Database - tr? local
spring.datasource.url=jdbc:mysql://localhost:3306/forumapp?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Redis - tr? local
spring.data.redis.host=localhost
spring.data.redis.port=6379

# JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# OTP
otp.expire.minutes=5

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vanthao10998@gmail.com
spring.mail.password=izsz ytpy hrmx njwy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Other
spring.jackson.default-property-inclusion=non_null

# Dùng H2 thay cho MySQL khi test
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# Vô hi?u hóa Redis ho?c dùng Mock n?u test authen không c?n cache th?c t?
spring.data.redis.host=none